{
  "name": "smartclaw-web",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "echo 'Dev mode: load individual scripts via index.html'"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
